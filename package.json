{
  "name": "sqlite-api",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "concurrently \"npm start --workspace=server\" \"npm start --workspace=client\"",
    "build": "concurrently \"npm start --workspace=server\" \"npm build --workspace=client\""
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  }
}
